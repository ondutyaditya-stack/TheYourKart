const domain=process.env.NEXT_PUBLIC_SHOPIFY_STORE_DOMAIN;
const storefrontAccessToken=process.env.NEXT_PUBLIC_SHOPIFY_STOREFRONT_ACCESS_TOKEN;
export async function shopifyFetch({query,variables}:{query:string;variables?:any}){const endpoint=`https://${domain}/api/2024-01/graphql.json`;try{const result=await fetch(endpoint,{method:"POST",headers:{"Content-Type":"application/json","X-Shopify-Storefront-Access-Token":storefrontAccessToken!},body:JSON.stringify({query,variables})});return{status:result.status,body:await result.json()}}catch(error){return{status:500,error:"Error"}}}
export async function getProducts(){const query="{products(first:10){edges{node{id title handle images(first:1){edges{node{url altText}}}priceRange{minVariantPrice{amount currencyCode}}}}}}";const response=await shopifyFetch({query});const edges=(response as any).body?.data?.products?.edges||[];return edges.map(({node}:any)=>({id:node.id,name:node.title,brand:"TheYourKart",price:parseFloat(node.priceRange.minVariantPrice.amount),originalPrice:parseFloat(node.priceRange.minVariantPrice.amount)*1.2,discount:20,rating:4.5,reviews:100,image:node.images.edges[0]?.node.url||"",category:"electronics",inStock:true}))}
